# Done to increase the memory available to Gradle.
org.gradle.jvmargs=-Xmx5G
org.gradle.parallel=true

# Mod properties
mod_version=0.4.0
maven_group=com.minersmarket
archives_name=minersmarket

# Release properties (used by gradle/shared release tasks)
release_project_name=Miner's Market
modrinth_project_id=6sCy4MMM
curseforge_project_id=1457302

# Target Minecraft version
target_mc_version=1.21.1

# Build plugin versions (selected by MC version in settings.gradle)
# MC 1.x uses Fabric Loom; MC 26.x+ uses the newer Fabric Loom plugin id.
fabric_loom_version=1.13.6
fabric_loom_26_version=1.15.5
# NeoForge (all 1.21.x and 26.x) and legacy Forge (1.20.1) use ModDevGradle.
moddevgradle_version=2.0.141
